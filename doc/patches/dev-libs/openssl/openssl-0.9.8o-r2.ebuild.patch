--- /auto/portage/dev-libs/openssl/openssl-0.9.8o-r2.ebuild	2010-10-11 03:05:34.000000000 -0400
+++ ./dev-libs/openssl/openssl-0.9.8o-r2.ebuild	2010-10-13 21:35:57.106053691 -0400
@@ -36,6 +36,3 @@
 
-src_unpack() {
-	unpack ${A}
-	cd "${S}"
-
+src_prepare() {
 	epatch "${FILESDIR}"/${PN}-0.9.7e-gentoo.patch
@@ -73,3 +70,3 @@
 
-src_compile() {
+src_configure() {
 	unset APPS #197996
@@ -127,3 +124,5 @@
 		Makefile || die
+}
 
+src_compile() {
 	# depend is needed to use $confopts

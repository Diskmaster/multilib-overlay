--- /auto/portage/dev-libs/libsigc++/libsigc++-1.0.4-r3.ebuild	2010-09-13 08:05:32.000000000 -0400
+++ ./dev-libs/libsigc++/libsigc++-1.0.4-r3.ebuild	2010-11-23 10:24:34.012915665 -0500
@@ -15,9 +15,7 @@
 
 DEPEND=""
 
-src_unpack() {
-	unpack ${A}
-	cd "${S}"
+src_prepare() {
 
 	epatch "${FILESDIR}"/${P}-gcc43.patch
 
@@ -30,15 +28,13 @@
 		scripts/ltconfig || die
 }
 
-src_compile() {
+src_configure() {
 	use debug \
 		&& myconf="--enable-debug=yes" \
 		|| myconf="--enable-debug=no"
 	econf ${myconf} || die "econf failed"
 
 	epatch "${FILESDIR}"/sandbox.patch
-
-	emake || die "emake failed"
 }
 
 src_install() {

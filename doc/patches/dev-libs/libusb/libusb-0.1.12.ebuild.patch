--- /auto/portage/dev-libs/libusb/libusb-0.1.12.ebuild	2010-03-30 22:36:03.000000000 -0400
+++ ./dev-libs/libusb/libusb-0.1.12.ebuild	2010-11-23 10:52:51.826079218 -0500
@@ -23,9 +23,7 @@
 	app-text/docbook-sgml-utils
 	~app-text/docbook-sgml-dtd-4.2 )"
 
-src_unpack() {
-	unpack ${A}
-	cd "${S}"
+src_prepare() {
 	sed -i -e 's:-Werror::' Makefile.am
 	epatch "${FILESDIR}"/${PV}-fbsd.patch
 	eautoreconf
@@ -39,12 +37,11 @@
 	${S}/doc/manual.sgml
 }
 
-src_compile() {
+src_configure() {
 	econf \
 		$(use_enable debug debug all) \
 		$(use_enable doc build-docs) \
 		|| die "econf failed"
-	emake || die "emake failed"
 }
 
 src_install() {

--- /auto/portage/www-client/firefox/firefox-3.6.11.ebuild	2010-10-21 22:12:08.000000000 -0400
+++ ./www-client/firefox/firefox-3.6.11.ebuild	2010-10-22 21:16:23.688410981 -0400
@@ -122,7 +122,9 @@
 
-	linguas
-	for X in ${linguas}; do
-		# FIXME: Add support for unpacking xpis to portage
-		[[ ${X} != "en" ]] && xpi_unpack "${P}-${X}.xpi"
-	done
+	if is_final_abi; then
+		linguas
+		for X in ${linguas}; do
+			# FIXME: Add support for unpacking xpis to portage
+			[[ ${X} != "en" ]] && xpi_unpack "${P}-${X}.xpi"
+		done
+	fi
 }

--- /usr/portage/www-client/mozilla-firefox/mozilla-firefox-3.6.8.ebuild	2010-07-24 06:49:32.000000000 -0500
+++ ./www-client/mozilla-firefox/mozilla-firefox-3.6.8.ebuild	2010-07-25 02:13:20.416887609 -0500
@@ -116,11 +116,13 @@
 src_unpack() {
 	unpack firefox-${MY_PV}.source.tar.bz2 ${PATCH}.tar.bz2
 
+	if is_final_abi; then
 	linguas
 	for X in ${linguas}; do
 		# FIXME: Add support for unpacking xpis to portage
 		[[ ${X} != "en" ]] && xpi_unpack "${P}-${X}.xpi"
 	done
+	fi
 }
 
 src_prepare() {

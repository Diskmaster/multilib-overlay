--- /auto/portage/sys-fs/fuse/fuse-2.7.4-r1.ebuild	2009-10-28 00:38:35.000000000 -0400
+++ ./sys-fs/fuse/fuse-2.7.4-r1.ebuild	2010-11-23 13:50:06.858349435 -0500
@@ -41,17 +41,18 @@
 	fi
 }
 
-src_unpack() {
-	unpack ${A}
-	cd "${S}"
+src_prepare() {
 	elibtoolize
 }
 
-src_compile() {
+src_configure() {
 	econf \
 		--disable-kernel-module \
 		--disable-example \
 		|| die "econf failed for fuse userland"
+}
+
+src_compile() {
 	emake || die "emake failed"
 
 	if use modules && use kernel_linux ; then

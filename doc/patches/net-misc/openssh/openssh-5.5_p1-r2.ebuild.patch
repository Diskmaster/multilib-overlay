--- /auto/portage/net-misc/openssh/openssh-5.5_p1-r2.ebuild	2010-09-24 07:36:46.000000000 -0400
+++ ./net-misc/openssh/openssh-5.5_p1-r2.ebuild	2010-11-23 17:53:22.581384826 -0500
@@ -140,6 +140,9 @@
 	echo "#define SSH_RELEASE ${v}" >>"${f}"
 	cp "${f}" version.h
 
+	# Use CC not LD otherwise invalid LDFLAGS get passed to ld
+	sed -i -e 's:$(LD):$(CC):' Makefile.in || die
+
 	eautoreconf
 }
 
